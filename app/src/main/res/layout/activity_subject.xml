<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_subject"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorMidNightBlue"
    tools:context="com.remswork.classmanager.SubjectActivity">

    <include layout="@layout/custom_toolbar"
        android:id="@+id/activity_subject_custom_toolbar">

    </include>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/activity_subject_custom_toolbar"
        android:id="@+id/activity_subject_layout">

    </RelativeLayout>

</RelativeLayout>